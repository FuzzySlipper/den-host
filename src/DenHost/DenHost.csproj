<Project Sdk="Microsoft.NET.Sdk.Worker">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <AssemblyName>den-host</AssemblyName>
    <RootNamespace>DenHost</RootNamespace>
    <OutputType>Exe</OutputType>
    <!--
      den-host is a Worker Service / Generic Host style binary.
      It also exposes a small CLI/health surface for one-shot
      operations. No HTTP server is hosted in this assembly.
    -->
  </PropertyGroup>

  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\DenHost.Harness\DenHost.Harness.csproj" />
  </ItemGroup>

</Project>
